[project]
name = "design-jobs-india"
version = "0.1.0"
description = "Aggregates design jobs, internships and freelance gigs across India from many sources into one searchable place."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]
